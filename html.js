const todoList = document.querySelector('.todo-list');

const buttonHtml = '<button>Click me!</button>';

// Додає елемент всередину списку на початок
// todoList.insertAdjacentHTML('afterbegin', buttonHtml);

// Додає елемент всередину списку на кінець
// todoList.insertAdjacentHTML('beforeend', buttonHtml);
// todoList.innerHTML += buttonHtml;

// Додає елемент перед списком
// todoList.insertAdjacentHTML('beforebegin', buttonHtml);

// Додає елемент після списку
// todoList.insertAdjacentHTML('afterend', buttonHtml);
